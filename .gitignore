# IDE
.idea/
.vscode/

# Claude Code
.claude/

# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
.env
.env.*
!.env.example
!.env.local.example

# Node
node_modules/
.next/
.vercel/
*.tsbuildinfo

# OpenAPI codegen intermediate — dumped from the backend spec, consumed by
# `make gen-api-types` to regenerate `frontend/src/lib/api-types.ts` (which IS
# committed). Rebuilt each run, never hand-edited.
frontend/openapi.json

# OS
.DS_Store
Thumbs.db

# Local media storage (LocalStorage backend)
.local-storage/
.vercel

# MkDocs build output (docs.vidit.app deploy artifact)
site/

# Working scratch — local notes + backups (not the tracked planning/ tree). Never commit.
tmp/

# Raw X API timeline pulls / datasets — tweet content, large. Never commit.
backend/datasets/

# The maintainer's personal X archive used by the promo take (video/record-v04.js).
/Vidit stuff.zip
