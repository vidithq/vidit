// Mirror of root twitter-image for the `/about` route — same image,
// same reasoning as the sibling `opengraph-image.tsx`. Literal
// `runtime` for the same reason that file documents.
export const runtime = "nodejs";
export { size, contentType, alt } from "../opengraph-image";
export { default } from "../opengraph-image";
