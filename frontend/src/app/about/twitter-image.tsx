// Mirror of root twitter-image for the `/about` route — same image,
// same reasoning as the sibling `opengraph-image.tsx`.
export { runtime, size, contentType, alt } from "../opengraph-image";
export { default } from "../opengraph-image";
